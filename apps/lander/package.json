{
	"name": "@ccproxy/lander",
	"version": "1.0.0",
	"description": "Landing page for ccproxy - The ultimate Claude Code proxy for power users",
	"private": true,
	"scripts": {
		"build": "mkdir -p dist && cp -r src/* dist/",
		"preview": "bunx serve src"
	},
	"keywords": [
		"ccproxy",
		"landing-page",
		"static-site"
	],
	"author": "snipeship",
	"license": "MIT"
}
